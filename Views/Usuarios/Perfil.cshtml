@model inmobiliaria.Models.Usuario

@{
    ViewData["Title"] = "Perfil de Usuario";
}

<div class="container my-5">
    <h1>@ViewData["Title"]</h1>
    <div class="row mt-4">
        <!-- Columna Izquierda: Avatar (clicable) e Info Básica -->
        <div class="col-lg-4 mb-4">
            <div class="card shadow-sm border-0">
                <div class="card-body text-center p-4">
                    <a href="#" data-bs-toggle="modal" data-bs-target="#avatarModal" title="Cambiar avatar">
                        @if (!string.IsNullOrEmpty(Model.Avatar))
                        {
                            <img src="@Url.Content(Model.Avatar)" alt="Avatar" class="rounded-circle img-fluid mb-3" style="width: 150px; height: 150px; object-fit: cover; cursor: pointer;">
                        }
                        else
                        {
                            <img src="@Url.Content("~/images/default-avatar.png")" alt="Avatar por defecto" class="rounded-circle img-fluid mb-3" style="width: 150px; height: 150px; object-fit: cover; cursor: pointer;">
                        }
                    </a>
                    <a href="#" data-bs-toggle="modal" data-bs-target="#avatarModal" title="Cambiar avatar"> Cambiar Avatar
                    </a>
                    <br />
                    <a asp-action="EliminarAvatar" asp-controller="Usuarios" asp-route-id="@Model.Id"> Eliminar Avatar</a>
                    
                    <h4 class="fw-bold">@Model.Nombre @Model.Apellido</h4>
                    <p class="text-muted mb-2">@Model.Email</p>
                    <span class="badge bg-@(Model.Rol == 1 ? "danger" : "info") fs-6">@Model.RolNombre</span>
                </div>
            </div>
        </div>

        <!-- Columna Derecha: Formulario de Edición de Datos -->
        <div class="col-lg-8">
            <div class="card shadow-sm border-0">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0 fw-bold">
                        <i class="fas fa-user-edit me-2"></i>Editar Información del Perfil
                    </h5>
                </div>
                <div class="card-body p-4">
                    <form asp-controller="Usuarios" asp-action="EditarPerfil" method="POST">
                        <div class="text-danger" asp-validation-summary="ModelOnly"></div>
                        <input type="hidden" asp-for="Id" />
                        <input type="hidden" asp-for="Clave" />
                        <input type="hidden" asp-for="Rol" />
                        <input type="hidden" asp-for="Avatar" />

                        <div class="row">
                            <div class="col-md-6 mb-3">
                                <label asp-for="Nombre" class="form-label fw-semibold">Nombre</label>
                                <input class="form-control" asp-for="Nombre">
                                <span asp-validation-for="Nombre" class="text-danger"></span>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label asp-for="Apellido" class="form-label fw-semibold">Apellido</label>
                                <input class="form-control" asp-for="Apellido">
                                <span asp-validation-for="Apellido" class="text-danger"></span>
                            </div>
                            <div class="col-md-12 mb-3">
                                <label asp-for="Email" class="form-label fw-semibold">Email</label>
                                <input class="form-control" asp-for="Email">
                                <span asp-validation-for="Email" class="text-danger"></span>
                            </div>
                        </div>
                        <hr>
                        <button type="submit" class="btn btn-primary">Guardar Datos</button>
                        <a asp-action="CambiarClave" asp-route-id="@Model.Id" class="btn btn-warning">Cambiar Contraseña</a>
                    </form>
                    </div>
                </div>
            </div>
    </div>
</div>

<!-- Modal para Cambiar Avatar -->
<div class="modal fade" id="avatarModal" tabindex="-1" aria-labelledby="avatarModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="avatarModalLabel">Cambiar Avatar</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form asp-controller="Usuarios" asp-action="EditarAvatar" method="post" enctype="multipart/form-data">
                <div class="modal-body">
                    <div class="form-group">
                        <label for="AvatarFile" class="control-label">Seleccionar nueva imagen</label>
                        <input type="file" name="AvatarFile" class="form-control" required />
                    </div>
                    @* <input type="hidden" asp-for="Id"> *@
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-primary">Guardar Avatar</button>
                </div>
            </form>
        </div>
    </div>
</div>
              
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}